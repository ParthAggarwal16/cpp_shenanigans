#include <cstring>
#include <iostream>

class Buffer {
private:
  char *data; // basicallt represents a pointer which can store an adrress of a
              // char object
};

int main() { return 0; }
